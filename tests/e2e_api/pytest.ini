[pytest]
# Standalone config for E2E API contract tests — independent of parent
# tests/ conftest (which pulls in heavy in-process DB fixtures we don't use).
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = .
addopts = -v --tb=short
