"""
AI константы для Market Bot.

Централизованные настройки для AI генерации через OpenRouter.
"""

# ──────────────────────────────────────────────────────────────
# Модели OpenRouter
# ──────────────────────────────────────────────────────────────

# Бесплатная модель по умолчанию (Qwen Coder)
FREE_MODEL = "qwen/qwen3-coder:free"

# Fallback модель при rate limit
FALLBACK_MODEL = "qwen/qwen3-coder:free"

# Платная модель (для будущего использования)
PAID_MODEL = "qwen/qwen-plus"

# ──────────────────────────────────────────────────────────────
# Параметры генерации
# ──────────────────────────────────────────────────────────────

# Максимальное количество токенов для ответа
# Оптимально для рекламных текстов 300-600 символов (~400-800 токенов)
AI_MAX_TOKENS = 600

# Температура генерации (0.0 - детерминировано, 1.0 - креативно)
AI_TEMPERATURE = 0.7

# ──────────────────────────────────────────────────────────────
# Лимиты генераций по тарифам (в месяц)
# ──────────────────────────────────────────────────────────────

FREE_AI_GENERATIONS_LIMIT = 0
STARTER_AI_GENERATIONS_LIMIT = 0
PRO_AI_GENERATIONS_LIMIT = 5
BUSINESS_AI_GENERATIONS_LIMIT = 20

# ──────────────────────────────────────────────────────────────
# Системные промпты для разных тематик
# ──────────────────────────────────────────────────────────────

TOPIC_PROMPTS = {
    "education": """Ты — копирайтер для образовательных проектов.
Пиши короткие, энергичные тексты про курсы и обучение.
Стиль: мотивирующий, с акцентом на результат.
Длина: 400-700 символов.
Структура: заголовок → боль → решение → призыв.
Эмодзи: уместно, 3-5 штук.""",
    "retail": """Ты — копирайтер для розничной торговли.
Пиши короткие, эмоциональные тексты про товары и услуги.
Стиль: дружеский, с акцентом на выгоду.
Длина: 300-600 символов.
Структура: эмоция → преимущество → скидка → призыв.
Эмодзи: умеренно, 2-4 штуки.""",
    "finance": """Ты — копирайтер для финансовых услуг.
Пиши лаконичные, убедительные тексты.
Стиль: профессиональный, но доступный.
Длина: 400-700 символов.
Структура: проблема → решение → гарантии → призыв.
Эмодзи: минимально, 1-3 штуки.""",
    "default": """Ты — профессиональный копирайтер для Telegram.
Пиши короткие, цепляющие рекламные тексты.
Стиль: живой, не шаблонный.
Длина: 300-600 символов (оптимально для Telegram).
Структура: заголовок → преимущества → призыв к действию.
Эмодзи: уместно, но без фанатизма.""",
}

# Системный промпт по умолчанию
DEFAULT_SYSTEM_PROMPT = TOPIC_PROMPTS["default"]

__all__ = [
    # Models
    "FREE_MODEL",
    "FALLBACK_MODEL",
    "PAID_MODEL",
    # Generation params
    "AI_MAX_TOKENS",
    "AI_TEMPERATURE",
    # Limits
    "FREE_AI_GENERATIONS_LIMIT",
    "STARTER_AI_GENERATIONS_LIMIT",
    "PRO_AI_GENERATIONS_LIMIT",
    "BUSINESS_AI_GENERATIONS_LIMIT",
    # Prompts
    "TOPIC_PROMPTS",
    "DEFAULT_SYSTEM_PROMPT",
]
